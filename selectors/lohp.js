var username = '.login_field.remove-shadow.input[aria-label="login-box"]';
var password = '.login_field.remove-shadow.input[aria-label="password-box"]';
var submit = '.btn-green.btn.login-btn.btn-primary';

module.exports = {
    username, password, submit
}